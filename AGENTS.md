# RTSGame0 项目说明

## 项目定位

这是一个即时策略/放置进攻类战斗原型，玩法方向参考《海岛奇兵》《部落冲突》的单场战斗：

- 玩家在指定部署区域放置兵种。
- 兵种部署后自动寻找目标、移动并攻击。
- 防守建筑会自动攻击进攻单位。
- 通过摧毁敌方建筑完成战斗目标。

当前目标不是做完整商业级系统，而是先完成一条可玩的战斗闭环。

## 当前技术形态

- 项目目前是一个纯前端 HTML/Canvas 原型。
- 入口文件是 `index.html`。
- 不依赖构建工具、不依赖包管理器、不需要启动开发服务器。
- 直接用浏览器打开 `D:\project-folder\RTSGame0\index.html` 即可试玩。

## 已实现内容

### 地图与部署

- 当前地图已扩展为大世界地图，Canvas 只是可拖动视口。
- 玩家可以按住地图拖动视角。
- 已从单独左侧部署改为四面边缘部署。
- 右侧为敌方基地区域。
- 地图上绘制了水域、部署区、基地地面、路径等简单视觉元素。
- 新增泥地，单位经过时会减速。
- 新增石头、树林作为不可通行障碍。
- 新增可破坏木墙，单位可以攻击摧毁。
- 新增两种开局流程：
  - 系统地图：直接攻打预设基地。
  - 自定义布防：玩家先摆放建筑和围墙，再开始进攻。
- 自定义布局可以保存到浏览器本地存储，并可读取恢复。

### 自定义布防

- 右侧面板可以在“系统地图”和“自定义布防”之间切换。
- 自定义布防阶段不会开始计时，也不能部署兵种。
- 玩家可以选择并摆放：
  - 民房。
  - 仓库。
  - 兵营。
  - 箭塔。
  - 机枪塔。
  - 炮塔。
  - 木墙。
- 建筑只能摆在基地黄色区域内。
- 建筑不能与已有建筑、墙体、石头或树林重叠。
- 至少摆放一个非墙体建筑后，才能点击“开始进攻”。
- 开始进攻后进入正常战斗阶段，玩家再从四面部署区放兵。
- “保存布局”会将当前自定义布防保存到 `localStorage`。
- “读取布局”会恢复上次保存的自定义布防。

### 建筑

已实现多种建筑：

- 普通建筑：只作为被攻击目标。
- 箭塔：基础单体防御建筑。
- 机枪塔：高射速、低单次伤害。
- 炮塔：低射速、高伤害，并带小范围溅射。
- 木墙：可破坏障碍物，不计入胜利目标。
- 兵营：被摧毁时会爆出现有兵种作为防御者。

建筑具备：

- 血量。
- 血条显示。
- 被攻击扣血。
- 血量归零后变成废墟。
- 全部建筑被摧毁后触发胜利。

### 兵种

已实现 3 种兵种：

- 近战步兵。
- 远程射手。
- 重装坦克。

单位具备：

- 点击部署。
- 血量。
- 血条显示。
- 自动寻找目标。
- 自动寻路并避开障碍。
- 进入攻击范围后自动攻击。
- 死亡后不再行动。
- 坦克会更倾向于优先攻击防御建筑。
- 防御者复用现有兵种，目前兵营会生成近战步兵和远程射手。
- 防御者属于防守方，会追击进攻者；防御塔不会攻击防御者。

### 战斗规则

- 玩家初始可部署 12 名近战步兵、8 名远程射手、3 辆重装坦克。
- 战斗倒计时为 3 分钟。
- 所有主要建筑和防御建筑被摧毁：胜利。
- 所有兵力部署完且全部死亡：失败。
- 时间耗尽：失败。
- 右侧 UI 显示剩余兵力、场上单位、建筑摧毁进度、倒计时和战斗状态。
- 有重新开始按钮。
- 右侧 UI 可以选择当前部署兵种。

### 音效

当前使用浏览器 Web Audio 生成简单音效，不依赖外部音频文件。

已包含：

- 近战攻击声。
- 射击/防御塔开火声。
- 炮击/爆炸声。
- 命中声。
- 建筑摧毁声。
- 胜利/失败提示声。

浏览器要求用户交互后才能播放音频，所以音效会在第一次点击部署后解锁。

## 主要代码结构

当前所有逻辑都在 `index.html` 中，分为以下几类函数：

- 初始化与状态：
  - `createInitialState`
  - `makeStructure`
  - `resetGame`
  - `renderUnitButtons`
  - `updateUi`

- 部署：
  - `getPointerPosition`
  - `isInsideRect`
  - `isInDeployZone`
  - `isBlockedAt`
  - `deployUnit`

- 战斗逻辑：
  - `update`
  - `updateUnits`
  - `updateDefenseStructures`
  - `updateProjectiles`
  - `damageStructure`
  - `damageUnit`
  - `checkBattleEnd`

- 目标选择与数学辅助：
  - `findTargetForUnit`
  - `findPathToStructure`
  - `aStar`
  - `nearestPointOnRect`
  - `distanceBetween`
  - `centerOf`
  - `clamp`

- 音效：
  - `unlockAudio`
  - `playSound`

- 绘制：
  - `draw`
  - `drawMap`
  - `drawBuildings`
  - `drawTroops`
  - `drawProjectiles`
  - `drawEffects`
  - `drawOutcome`

## 验证记录

已做过一次基础语法检查：

```powershell
node -e "const html=require('fs').readFileSync('index.html','utf8'); const script=html.match(/<script>([\s\S]*)<\/script>/)[1]; new Function(script); console.log('script parsed')"
```

检查结果：

```text
script parsed
```

说明 `index.html` 内嵌 JavaScript 没有基础语法错误。

最近一次新增多兵种、多防御塔、地形障碍、四面部署和音效后，也通过了同样的语法检查，并额外做过一次 Node 伪 DOM 运行时冒烟检查：

```text
script parsed
runtime smoke passed
```

新增“系统地图 / 自定义布防”流程后，又做过一次功能流检查：

```text
custom flow passed
```

新增大地图拖动、布局保存、强化炮塔/机枪塔、兵营爆防御者和炮塔寻路修复后，做过以下检查：

```text
script parsed
runtime smoke passed
feature flow passed
cannon path passed
```

## 后续开发建议

建议按以下顺序继续扩展：

1. 拆分文件：将 HTML、CSS、JS 分离为 `index.html`、`styles.css`、`src/main.js`。
2. 继续拆分配置：将兵种、建筑、地图数据从 `index.html` 抽到独立数据文件。
3. 增加更多兵种：爆破兵、医疗兵、火箭兵。
4. 增加更多建筑：大本营、资源建筑、迫击炮、护盾发生器。
5. 优化寻路系统：当前是轻量网格 A*，后续可以增加路径缓存、拥挤避让和墙体优先破坏判断。
6. 增加部署限制：兵种人口、部署冷却、兵种组合上限。
7. 增加结算表现：星级、摧毁百分比、奖励。
8. 增加关卡数据：用配置生成不同基地布局。

## 开发约定

- 当前阶段优先保证“能玩”和“逻辑清楚”，不要过早复杂化。
- UI 可以简单绘制，但要保证兵种、建筑、部署区、攻击反馈容易辨认。
- 新增功能应先保持单场战斗闭环稳定。
- 改动后建议至少重新执行一次 JavaScript 语法检查。
- 如果后续引入构建工具，再补充运行、测试和打包命令。
